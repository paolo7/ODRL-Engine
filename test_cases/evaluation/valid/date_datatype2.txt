should be allowed as the action refinement on a date object is satisfied on every row, as they have a publication date with equal timestamp, even though the string representation of that timestamp is different
date_datatype
